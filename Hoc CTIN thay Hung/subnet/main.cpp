#include <bits/stdc++.h>

using namespace std;
string sns,s;

int main(){
    cin.sync_with_stdio(false);
    cin.tie(0);
    freopen("","r",stdin);
    freopen("","w",stdout);
    cin>>n>>m;
    cin>>sns;
    for (int i=0;i<m;i++){
        cin>>s;

    }
    return 0;
}
